# P0 — Socle V1 Alaska Pilot — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stabiliser le socle de sécurité et de données de l'application avant tout vrai lancement V1 : supprimer l'ancien système d'auth, migrer les rôles vers une table `profiles` sécurisée, corriger le schéma SQL, et supprimer le seed automatique au runtime.

**Architecture:** On supprime complètement la couche legacy (cookie `alaska_session`, routes `api/auth/*`, `lib/server/auth.ts`, `lib/server/pilot-store.ts`) et on normalise le contrôle d'accès sur une table `profiles` avec RLS. Le middleware et le layout lisent le rôle depuis `profiles` via Supabase, et toutes les policies Supabase utilisent une fonction SQL `auth_user_role()` SECURITY DEFINER pour éviter que le client puisse modifier son propre rôle.

**Tech Stack:** Next.js App Router, Supabase (Auth + Postgres + RLS), TypeScript, Vitest

---

## Fichiers concernés

**À créer :**
- `supabase/migrations/20260410000000_p0_profiles_schema_fixes.sql`
- `lib/server/db-types.ts`

**À modifier :**
- `lib/server/supabase-store.ts` — import depuis db-types, retirer seedIfEmpty
- `lib/server/analytics.ts` — import depuis db-types
- `lib/supabase/middleware.ts` — retirer legacy fallback, ajouter profiles lookup, ajouter `/` en admin-only
- `app/(dashboard)/layout.tsx` — lire le rôle depuis profiles
- `app/api/dashboard/route.ts` — retirer seedIfEmpty
- `app/api/week/route.ts` — retirer seedIfEmpty
- `app/api/reporting/route.ts` — retirer seedIfEmpty
- `app/api/reporting/export/route.ts` — retirer seedIfEmpty
- `app/api/charges/route.ts` — retirer seedIfEmpty
- `app/api/objectives/route.ts` — retirer seedIfEmpty
- `app/api/caisse/balance/route.ts` — retirer seedIfEmpty
- `tests/middleware.test.ts` — réécrire pour le chemin Supabase

**À supprimer :**
- `app/api/auth/login/route.ts`
- `app/api/auth/logout/route.ts`
- `app/api/auth/session/route.ts`
- `lib/server/auth.ts`
- `lib/server/pilot-store.ts`

---

## Task 1 : Déplacer les types partagés vers `lib/server/db-types.ts`

**Objectif :** Extraire les interfaces `DailySaleRecord`, `ExpenseRecord`, `PilotDb` de `pilot-store.ts` vers un fichier dédié, pour pouvoir supprimer `pilot-store.ts` ensuite.

**Fichiers :**
- Créer : `lib/server/db-types.ts`
- Modifier : `lib/server/supabase-store.ts`
- Modifier : `lib/server/analytics.ts`

- [ ] **Step 1 : Créer `lib/server/db-types.ts`**

```typescript
// lib/server/db-types.ts
import "server-only"

import type { ActionItem, DailyEntry, FixedCharge, ImportRecord, MonthlyObjective, Objective } from "@/lib/types"

export interface DailySaleRecord {
  id: string
  date: string
  ca_caisse: number
  ca_b2b: number
  ca_soir: number
  pct_soir: number
  tickets_count: number
  mouvement_caisse: number
  notes: string
  source: "manual" | "csv_import"
  import_id: string | null
  created_by: string | null
  updated_at: string
}

export interface ExpenseRecord {
  id: string
  date: string
  category: string
  label: string
  amount: number
  notes: string
  created_by: string | null
  updated_at: string
}

export interface PilotDb {
  users: []
  daily_sales: DailySaleRecord[]
  expenses: ExpenseRecord[]
  fixed_charges: FixedCharge[]
  objectives: Objective[]
  monthly_objectives: MonthlyObjective[]
  action_items: ActionItem[]
  import_history: ImportRecord[]
}
```

> Note : Copiez les définitions exactes depuis `lib/server/pilot-store.ts` en vérifiant champ par champ. Le champ `mouvement_caisse: number` doit être présent dans `DailySaleRecord`.

- [ ] **Step 2 : Mettre à jour l'import dans `lib/server/supabase-store.ts`**

Remplacer la ligne :
```typescript
import type { DailySaleRecord, ExpenseRecord, PilotDb } from "@/lib/server/pilot-store"
```
Par :
```typescript
import type { DailySaleRecord, ExpenseRecord, PilotDb } from "@/lib/server/db-types"
```

- [ ] **Step 3 : Mettre à jour l'import dans `lib/server/analytics.ts`**

Remplacer la ligne :
```typescript
import type { DailySaleRecord, ExpenseRecord, PilotDb } from "@/lib/server/pilot-store"
```
Par :
```typescript
import type { DailySaleRecord, ExpenseRecord, PilotDb } from "@/lib/server/db-types"
```

- [ ] **Step 4 : Vérifier que le build passe avant de supprimer les fichiers**

```bash
npm run build 2>&1 | tail -20
```

Résultat attendu : pas d'erreur TypeScript liée aux imports de types.

---

## Task 2 : Supprimer le système d'auth legacy

**Objectif :** Retirer `app/api/auth/*`, `lib/server/auth.ts`, `lib/server/pilot-store.ts`. Ces fichiers ne sont plus appelés depuis l'UI (qui utilise Supabase Auth depuis la page login). Les routes legacy existent mais sont mortes.

**Fichiers :**
- Supprimer : `app/api/auth/login/route.ts`
- Supprimer : `app/api/auth/logout/route.ts`
- Supprimer : `app/api/auth/session/route.ts`
- Supprimer : `lib/server/auth.ts`
- Supprimer : `lib/server/pilot-store.ts`

- [ ] **Step 1 : Supprimer les fichiers legacy**

```bash
rm app/api/auth/login/route.ts app/api/auth/logout/route.ts app/api/auth/session/route.ts
rm lib/server/auth.ts lib/server/pilot-store.ts
```

Si le répertoire `app/api/auth/` est désormais vide, le supprimer :
```bash
rmdir app/api/auth/login app/api/auth
```

- [ ] **Step 2 : Vérifier qu'il n'existe plus de références aux fichiers supprimés**

```bash
grep -r "lib/server/auth" app/ lib/ --include="*.ts" --include="*.tsx" -l
grep -r "lib/server/pilot-store" app/ lib/ --include="*.ts" --include="*.tsx" -l
grep -r "alaska_session" app/ lib/ --include="*.ts" --include="*.tsx" -l
```

Résultat attendu : aucun fichier (hors `lib/supabase/middleware.ts` qui sera traité à la Task 4).

- [ ] **Step 3 : Vérifier que le build passe**

```bash
npm run build 2>&1 | tail -20
```

Résultat attendu : build OK.

- [ ] **Step 4 : Commit**

```bash
git add -A
git commit -m "refactor: remove legacy auth system (pilot-store, alaska_session, api/auth routes)"
```

---

## Task 3 : Migration SQL — profiles, mouvement_caisse, RLS complet

**Objectif :** Une nouvelle migration qui (1) crée la table `profiles` avec trigger auto-création, (2) ajoute `mouvement_caisse` à `daily_sales`, (3) active RLS sur les tables oubliées, (4) ajoute des policies manquantes, (5) remplace toutes les policies `user_metadata` par une fonction `auth_user_role()` SECURITY DEFINER.

**Fichiers :**
- Créer : `supabase/migrations/20260410000000_p0_profiles_schema_fixes.sql`

- [ ] **Step 1 : Écrire la migration**

Créer le fichier `supabase/migrations/20260410000000_p0_profiles_schema_fixes.sql` avec le contenu suivant :

```sql
-- Migration P0: profiles, mouvement_caisse, RLS complet
-- Date: 2026-04-10

-- =========================================
-- 1. Ajouter mouvement_caisse à daily_sales
-- =========================================
ALTER TABLE public.daily_sales
  ADD COLUMN IF NOT EXISTS mouvement_caisse NUMERIC(10,2) NOT NULL DEFAULT 0;

-- =========================================
-- 2. Table profiles (rôle stocké côté serveur)
-- =========================================
CREATE TABLE IF NOT EXISTS public.profiles (
  id   UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  role TEXT NOT NULL DEFAULT 'manager' CHECK (role IN ('admin', 'manager')),
  name TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

-- Un utilisateur peut lire son propre profil, personne d'autre
CREATE POLICY "users_read_own_profile" ON public.profiles
  FOR SELECT USING (auth.uid() = id);

-- Seul le service_role peut écrire (trigger, bootstrap)
-- Pas de policy INSERT/UPDATE/DELETE pour les rôles normaux → bloqué par défaut

-- =========================================
-- 3. Trigger auto-création profil à l'inscription
-- =========================================
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, role, name)
  VALUES (
    NEW.id,
    COALESCE(NEW.raw_user_meta_data->>'role', 'manager'),
    NEW.raw_user_meta_data->>'name'
  )
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = public;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- Backfill des utilisateurs existants
INSERT INTO public.profiles (id, role, name)
SELECT
  id,
  COALESCE(raw_user_meta_data->>'role', 'manager'),
  raw_user_meta_data->>'name'
FROM auth.users
ON CONFLICT (id) DO NOTHING;

-- =========================================
-- 4. Fonction SECURITY DEFINER pour lire le rôle
-- =========================================
CREATE OR REPLACE FUNCTION public.auth_user_role()
RETURNS TEXT AS $$
  SELECT role FROM public.profiles WHERE id = auth.uid();
$$ LANGUAGE SQL STABLE SECURITY DEFINER SET search_path = public;

-- =========================================
-- 5. RLS sur tables oubliées
-- =========================================
ALTER TABLE public.monthly_objectives ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.charge_history ENABLE ROW LEVEL SECURITY;

-- =========================================
-- 6. Réécriture des policies avec auth_user_role()
-- =========================================

-- daily_sales
DROP POLICY IF EXISTS "admin_full_access" ON public.daily_sales;
DROP POLICY IF EXISTS "manager_read_daily_sales" ON public.daily_sales;

CREATE POLICY "admin_full_access_daily_sales" ON public.daily_sales
  FOR ALL USING (public.auth_user_role() = 'admin');

CREATE POLICY "manager_read_daily_sales" ON public.daily_sales
  FOR SELECT USING (public.auth_user_role() = 'manager');

-- Le manager peut insérer et mettre à jour daily_sales (saisie quotidienne)
CREATE POLICY "manager_insert_daily_sales" ON public.daily_sales
  FOR INSERT WITH CHECK (public.auth_user_role() = 'manager');

CREATE POLICY "manager_update_daily_sales" ON public.daily_sales
  FOR UPDATE USING (public.auth_user_role() = 'manager');

-- expenses
DROP POLICY IF EXISTS "admin_expenses" ON public.expenses;
DROP POLICY IF EXISTS "manager_own_expenses" ON public.expenses;

CREATE POLICY "admin_expenses" ON public.expenses
  FOR ALL USING (public.auth_user_role() = 'admin');

CREATE POLICY "manager_own_expenses" ON public.expenses
  FOR ALL USING (
    created_by = auth.uid()
    AND public.auth_user_role() = 'manager'
  );

-- fixed_charges
DROP POLICY IF EXISTS "admin_fixed_charges" ON public.fixed_charges;
CREATE POLICY "admin_fixed_charges" ON public.fixed_charges
  FOR ALL USING (public.auth_user_role() = 'admin');

-- objectives
DROP POLICY IF EXISTS "admin_objectives" ON public.objectives;
CREATE POLICY "admin_objectives" ON public.objectives
  FOR ALL USING (public.auth_user_role() = 'admin');

-- monthly_objectives (policy manquante)
CREATE POLICY "admin_monthly_objectives" ON public.monthly_objectives
  FOR ALL USING (public.auth_user_role() = 'admin');

-- action_items
DROP POLICY IF EXISTS "admin_action_items" ON public.action_items;
CREATE POLICY "admin_action_items" ON public.action_items
  FOR ALL USING (public.auth_user_role() = 'admin');

-- pos_imports
DROP POLICY IF EXISTS "admin_pos_imports" ON public.pos_imports;
CREATE POLICY "admin_pos_imports" ON public.pos_imports
  FOR ALL USING (public.auth_user_role() = 'admin');

-- charge_history (table présente mais sans policy)
CREATE POLICY "admin_charge_history" ON public.charge_history
  FOR ALL USING (public.auth_user_role() = 'admin');
```

- [ ] **Step 2 : Appliquer la migration via Supabase MCP**

Utiliser l'outil `mcp__plugin_supabase_supabase__apply_migration` avec le contenu ci-dessus.

Ou via CLI si Supabase CLI est configuré :
```bash
supabase db push
```

- [ ] **Step 3 : Vérifier que la table profiles existe et contient les utilisateurs**

Via Supabase Dashboard → Table Editor → profiles, ou via l'outil `mcp__plugin_supabase_supabase__execute_sql` :
```sql
SELECT id, role, name FROM public.profiles;
```

Résultat attendu : une ligne par utilisateur existant (admin + manager).

- [ ] **Step 4 : Commit**

```bash
git add supabase/migrations/20260410000000_p0_profiles_schema_fixes.sql
git commit -m "feat: add profiles table, mouvement_caisse column, complete RLS with auth_user_role()"
```

---

## Task 4 : Mettre à jour le middleware Supabase

**Objectif :** Supprimer complètement le fallback legacy cookie. Lire le rôle depuis `profiles`. Ajouter `/` comme route admin-only.

**Fichiers :**
- Modifier : `lib/supabase/middleware.ts`

- [ ] **Step 1 : Réécrire `lib/supabase/middleware.ts`**

Remplacer tout le contenu du fichier par :

```typescript
import { createServerClient, type CookieOptions } from "@supabase/ssr"
import { NextResponse, type NextRequest } from "next/server"

// Routes accessibles à l'admin uniquement
const ADMIN_ROUTES = ["/", "/charges", "/objectifs", "/import", "/reporting"]

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({
    request: {
      headers: request.headers,
    },
  })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        get(name: string) {
          return request.cookies.get(name)?.value
        },
        set(name: string, value: string, options: CookieOptions) {
          request.cookies.set({ name, value, ...options })
          response = NextResponse.next({ request: { headers: request.headers } })
          response.cookies.set({ name, value, ...options })
        },
        remove(name: string, options: CookieOptions) {
          request.cookies.set({ name, value: "", ...options })
          response = NextResponse.next({ request: { headers: request.headers } })
          response.cookies.set({ name, value: "", ...options })
        },
      },
    }
  )

  const {
    data: { user },
  } = await supabase.auth.getUser()

  const pathname = request.nextUrl.pathname
  const isLoginPage = pathname.startsWith("/login")
  const isProtectedRoute =
    pathname === "/" ||
    pathname.startsWith("/saisie") ||
    pathname.startsWith("/semaine") ||
    pathname.startsWith("/charges") ||
    pathname.startsWith("/objectifs") ||
    pathname.startsWith("/import") ||
    pathname.startsWith("/reporting")

  // Pas connecté → login
  if (!user && isProtectedRoute) {
    const url = request.nextUrl.clone()
    url.pathname = "/login"
    url.searchParams.set("redirect", pathname)
    return NextResponse.redirect(url)
  }

  // Déjà connecté sur la page login → rediriger selon rôle
  if (user && isLoginPage) {
    const { data: profile } = await supabase
      .from("profiles")
      .select("role")
      .eq("id", user.id)
      .single()
    const role = (profile?.role || "manager") as "admin" | "manager"
    const url = request.nextUrl.clone()
    url.pathname = role === "admin" ? "/" : "/saisie"
    return NextResponse.redirect(url)
  }

  // Vérification admin-only pour les routes sensibles (y compris /)
  const isAdminRoute = ADMIN_ROUTES.some(
    (route) => pathname === route || (route !== "/" && pathname.startsWith(`${route}/`))
  )

  if (user && isAdminRoute) {
    const { data: profile } = await supabase
      .from("profiles")
      .select("role")
      .eq("id", user.id)
      .single()
    const role = (profile?.role || "manager") as "admin" | "manager"

    if (role !== "admin") {
      const url = request.nextUrl.clone()
      url.pathname = "/saisie"
      url.searchParams.set("forbidden", "1")
      return NextResponse.redirect(url)
    }
  }

  return response
}
```

- [ ] **Step 2 : Vérifier que le build passe**

```bash
npm run build 2>&1 | tail -20
```

Résultat attendu : build OK, pas d'erreurs TypeScript.

---

## Task 5 : Mettre à jour `app/(dashboard)/layout.tsx`

**Objectif :** Lire le rôle depuis `profiles` (serveur, sécurisé) plutôt que depuis `user_metadata`.

**Fichiers :**
- Modifier : `app/(dashboard)/layout.tsx`

- [ ] **Step 1 : Mettre à jour layout.tsx**

Remplacer tout le contenu par :

```typescript
import { redirect } from "next/navigation"
import { createClient } from "@/lib/supabase/server"
import { AppShell } from "@/components/layout/AppShell"

export default async function DashboardLayout({ children }: { children: React.ReactNode }) {
  const supabase = createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) redirect("/login")

  const { data: profile } = await supabase
    .from("profiles")
    .select("role, name")
    .eq("id", user.id)
    .single()

  const role = (profile?.role || "manager") as "admin" | "manager"
  const name = profile?.name || user.email?.split("@")[0] || "Utilisateur"

  return (
    <AppShell
      user={{
        email: user.email || "",
        name,
        role,
      }}
    >
      {children}
    </AppShell>
  )
}
```

- [ ] **Step 2 : Vérifier le build**

```bash
npm run build 2>&1 | tail -20
```

---

## Task 6 : Supprimer `seedIfEmpty` de toutes les routes API

**Objectif :** Le seed automatique au runtime injecte des données mock en base sur tout accès API avec une base vide. Pour la production, ce comportement doit être supprimé. Le seeding se fera via un script CLI dédié (`scripts/seed-initial-data.mjs`).

**Fichiers à modifier :**
- `lib/server/supabase-store.ts` — retirer l'option `seedIfEmpty` de `readSnapshot`
- `app/api/dashboard/route.ts`
- `app/api/week/route.ts`
- `app/api/reporting/route.ts`
- `app/api/reporting/export/route.ts`
- `app/api/charges/route.ts`
- `app/api/objectives/route.ts`
- `app/api/caisse/balance/route.ts`

- [ ] **Step 1 : Retirer l'option seedIfEmpty de `readSnapshot` dans `supabase-store.ts`**

Remplacer la signature et le début de `readSnapshot` :

```typescript
// Avant
export async function readSnapshot(client: SupabaseClientLike, options?: { seedIfEmpty?: boolean; userId?: string | null }): Promise<PilotDb> {
  if (options?.seedIfEmpty) {
    await ensureSeedData(client, options.userId || null)
  }
  // ...
}

// Après
export async function readSnapshot(client: SupabaseClientLike): Promise<PilotDb> {
  // ... (garder le reste identique)
}
```

La fonction `ensureSeedData` est conservée dans le fichier mais ne sera plus appelée automatiquement. Elle sera utilisée par un script de seed explicite.

- [ ] **Step 2 : Mettre à jour `app/api/dashboard/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 3 : Mettre à jour `app/api/week/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 4 : Mettre à jour `app/api/reporting/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 5 : Mettre à jour `app/api/reporting/export/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 6 : Mettre à jour `app/api/charges/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 7 : Mettre à jour `app/api/objectives/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 8 : Mettre à jour `app/api/caisse/balance/route.ts`**

Remplacer :
```typescript
const db = await readSnapshot(supabase, { seedIfEmpty: Boolean(user), userId: user?.id || null })
```
Par :
```typescript
const db = await readSnapshot(supabase)
```

- [ ] **Step 9 : Vérifier le build**

```bash
npm run build 2>&1 | tail -20
```

Résultat attendu : build OK, plus aucune référence à `seedIfEmpty`.

- [ ] **Step 10 : Commit**

```bash
git add -A
git commit -m "refactor: remove automatic seedIfEmpty from all API routes, seed is now explicit"
```

---

## Task 7 : Réécrire les tests middleware

**Objectif :** Les tests actuels testent le chemin legacy cookie (`alaska_session`). Ce chemin n'existe plus. Réécrire les tests pour mocker Supabase et tester le vrai flux.

**Fichiers :**
- Modifier : `tests/middleware.test.ts`

- [ ] **Step 1 : Réécrire `tests/middleware.test.ts`**

```typescript
import { describe, expect, it, vi, beforeEach } from "vitest"
import { NextRequest } from "next/server"

// Mock @supabase/ssr avant l'import du middleware
vi.mock("@supabase/ssr", () => ({
  createServerClient: vi.fn(),
}))

import { createServerClient } from "@supabase/ssr"
import { middleware } from "../middleware"

function makeSupabaseMock(user: { id: string } | null, role: string | null) {
  const profileData = role ? { role } : null
  return {
    auth: {
      getUser: vi.fn().mockResolvedValue({ data: { user }, error: null }),
    },
    from: vi.fn().mockReturnValue({
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockReturnThis(),
      single: vi.fn().mockResolvedValue({ data: profileData, error: null }),
    }),
    cookies: { get: vi.fn(), set: vi.fn(), remove: vi.fn() },
  }
}

beforeEach(() => {
  vi.clearAllMocks()
  // Set env vars so middleware doesn't fail
  process.env.NEXT_PUBLIC_SUPABASE_URL = "https://test.supabase.co"
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY = "test-anon-key"
})

describe("middleware access control", () => {
  it("redirige les anonymes vers /login sur une route protégée", async () => {
    vi.mocked(createServerClient).mockReturnValue(makeSupabaseMock(null, null) as any)
    const request = new NextRequest("http://localhost/charges")
    const response = await middleware(request)
    expect(response.headers.get("location")).toContain("/login")
  })

  it("redirige les anonymes vers /login sur /", async () => {
    vi.mocked(createServerClient).mockReturnValue(makeSupabaseMock(null, null) as any)
    const request = new NextRequest("http://localhost/")
    const response = await middleware(request)
    expect(response.headers.get("location")).toContain("/login")
  })

  it("redirige le manager hors des pages admin (/reporting)", async () => {
    vi.mocked(createServerClient).mockReturnValue(
      makeSupabaseMock({ id: "manager-id" }, "manager") as any
    )
    const request = new NextRequest("http://localhost/reporting")
    const response = await middleware(request)
    expect(response.headers.get("location")).toContain("/saisie")
  })

  it("redirige le manager hors du dashboard / (admin-only)", async () => {
    vi.mocked(createServerClient).mockReturnValue(
      makeSupabaseMock({ id: "manager-id" }, "manager") as any
    )
    const request = new NextRequest("http://localhost/")
    const response = await middleware(request)
    expect(response.headers.get("location")).toContain("/saisie")
  })

  it("laisse l'admin accéder au dashboard /", async () => {
    vi.mocked(createServerClient).mockReturnValue(
      makeSupabaseMock({ id: "admin-id" }, "admin") as any
    )
    const request = new NextRequest("http://localhost/")
    const response = await middleware(request)
    expect(response.headers.get("location")).toBeNull()
  })

  it("laisse l'admin accéder à /reporting", async () => {
    vi.mocked(createServerClient).mockReturnValue(
      makeSupabaseMock({ id: "admin-id" }, "admin") as any
    )
    const request = new NextRequest("http://localhost/reporting")
    const response = await middleware(request)
    expect(response.headers.get("location")).toBeNull()
  })

  it("laisse le manager accéder à /saisie", async () => {
    vi.mocked(createServerClient).mockReturnValue(
      makeSupabaseMock({ id: "manager-id" }, "manager") as any
    )
    const request = new NextRequest("http://localhost/saisie")
    const response = await middleware(request)
    expect(response.headers.get("location")).toBeNull()
  })
})
```

- [ ] **Step 2 : Lancer les tests et vérifier qu'ils passent**

```bash
npm run test -- tests/middleware.test.ts
```

Résultat attendu : 7 tests passent.

- [ ] **Step 3 : Lancer tous les tests**

```bash
npm run test
```

Résultat attendu : tous les tests passent.

- [ ] **Step 4 : Commit**

```bash
git add tests/middleware.test.ts
git commit -m "test: rewrite middleware tests for Supabase path, remove legacy cookie tests"
```

---

## Task 8 : Validation finale P0

- [ ] **Step 1 : Lancer les tests complets**

```bash
npm run test
```

Résultat attendu : tous les tests passent.

- [ ] **Step 2 : Lancer le lint**

```bash
npm run lint
```

Résultat attendu : aucune erreur.

- [ ] **Step 3 : Build de production**

```bash
npm run build
```

Résultat attendu : build OK sans erreurs TypeScript.

- [ ] **Step 4 : Vérifier l'absence de références résiduelles**

```bash
grep -r "alaska_session\|pilot-store\|lib/server/auth\|seedIfEmpty" app/ lib/ --include="*.ts" --include="*.tsx" -l
```

Résultat attendu : aucun fichier (ou seulement des commentaires dans des fichiers de documentation).

- [ ] **Step 5 : Commit final P0**

```bash
git add -A
git commit -m "feat: P0 socle V1 - auth cleanup, profiles table, schema fixes, no runtime seed"
```

---

## Notes pour le plan P1 (suite)

Le plan P0 ne couvre pas :
- Brancher FIXED_CHARGES mock sur la vraie base dans `saisie/page.tsx` et `WeekGrid.tsx`
- Compléter le module charges (date d'effet, historique, simulation)
- Compléter le module objectifs (édition, création d'actions)
- Compléter le reporting V1 (comparatif N-1, charges théoriques)
- Rendre l'import plus robuste (stockage CSV, encoding Latin-1)

Ces items font l'objet du plan P1 à rédiger après validation du P0.

Un script `scripts/seed-initial-data.mjs` devra être créé pour bootstrapper les données initiales sur une base vierge. Il appellera `ensureSeedData` depuis `supabase-store.ts` avec les credentials service_role.
